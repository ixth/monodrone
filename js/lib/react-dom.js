export default window.ReactDOM;

const {
    createPortal,
    findDOMNode,
    hydrate,
    render,
    unmountComponentAtNode,
    flushSync
} = window.ReactDOM;

export {
    createPortal,
    findDOMNode,
    hydrate,
    render,
    unmountComponentAtNode,
    flushSync
};
